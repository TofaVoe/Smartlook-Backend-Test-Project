// Collection Service
// Add story into collection

async function addStory(collectionId: number, storyId: number) {

}

// Fetch story with comments

// Note to trigger some if this with CRON



export {addStory}
